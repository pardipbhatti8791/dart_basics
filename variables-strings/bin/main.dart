main(List<String> arguments) { // entry point for execution
  // Variable = spot in memory. Or just a bucket to hold value

  //Type??
  /**
      String,
      Number (integers and doubles)
      Boolean
      lists
      maps
   */

  // var support all the types
  var country = 'India';  // string
  print(country);

  // define variable with types
  String name;
  name = "Gugu";
  print(name);
  
  // Numbers
  num age = 13;
  print(age);

  double anotherAge;
  anotherAge = 23.323;
  print(anotherAge);
  
  // Boolean
  bool guga;
  guga = true;
  print(guga);

}
